/**
 * xiyu 主题样式已外置到 /public/css/xiyu.css（唯一源，直接编辑那个文件）。
 * 由 pages/_document.js 以 <link rel="stylesheet"> 引入 <head>：
 * - head 中的样式表是渲染阻塞的，不会 FOUC
 * - 浏览器可跨页面缓存，HTML 每页减重 ~44KB
 * 此文件保留空壳导出，兼容旧 import。
 */
export const CSS = ''
export const Style = () => null
export default Style
